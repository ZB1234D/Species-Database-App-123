import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.raimatak.speciesdb',
  appName: 'Species Database App',
  webDir: 'www',
  server: {
    url: "srv-d5qa2kn5r7bs738g00d0",
    cleartext: false
  }
};

export default config;
